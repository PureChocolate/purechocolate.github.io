// Copy this file to config.js and fill in your Worker URL
// config.js is gitignored — it won't be committed
window.TTTConfig = {
  API_BASE: ''  // e.g. 'https://ttt-multiplayer.yourname.workers.dev'
};
